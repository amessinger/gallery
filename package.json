{
  "scripts": {
    "serve": "yarn eleventy --serve",
    "build": "yarn eleventy",
    "deploy": "node deploy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^0.8.3",
    "autoprefixer": "^9.7.1",
    "fs-extra": "^8.1.0",
    "ftp-deploy": "^2.3.3",
    "marked": "^0.7.0",
    "postcss": "^7.0.21",
    "sharp": "^0.22.1"
  },
  "dependencies": {
    "tailwindcss": "^1.1.3"
  }
}
